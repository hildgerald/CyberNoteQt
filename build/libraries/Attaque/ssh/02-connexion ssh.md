# connexion avec clé privé
```
ssh -i <cle_prive> <nom_utilisateur>@<IP>
ex :
ssh -i id_rsa root@192.168.56.2


```

# connexion avec clé privé et nouveau port
```
ssh -i <cle_prive> -p <port> <nom_utilisateur>@<IP>
ex :
ssh -i id_rsa -p 2222 root@192.168.56.2


```
